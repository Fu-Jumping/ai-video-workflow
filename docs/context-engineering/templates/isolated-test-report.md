# 隔离测试报告模板（isolated test report）

> 用途：隔离端到端测试 / 复测的报告骨架。结构对齐机器全局 skill `avw-isolated-e2e-testing` 的 REPORT.md 模板；红线与编排流程见 `docs/context-engineering/06-verification-gates.md` §4。测试目录：`G:\develop-G\tests\<轮次目录>\`。

# <项目名> 测试报告（第 N 轮）

- 测试日期：
- 测试根目录：
- 被测版本（`git log --oneline -1` 输出的提交号）：
- 测试范围：（R1 主链路 / R2 审核修改链路 / R3 真实环境走查 / 复测矩阵项）
- 是否消耗生成额度：（必须为"否"，除非用户明确允许）

## 一、环境与初始化

- 克隆来源与版本确认表：
- 运行环境（git / node / pnpm 版本）：
- 已知环境问题：

## 二、场景执行记录

（每场景：命令、结果、问题与解决、耗时）

## 三、问题清单

（分级：阻断 / 严重 / 一般 / 提示；注明**工具缺陷 vs 操作不当**；给最小复现路径）

## 四、产物路径与文件树

## 五、各场景结论表

## 六、总体评级

（可用 / 有条件可用 / 不可用，附理由）

## 附：执行时间线摘要

---

## 主对话终审区（测试结束后填写）

- 比对结论（与参考项的差异，仅主环境可见）：
- 问题分类复核：
- 后续动作（修复 / 复测 / 归档）：
