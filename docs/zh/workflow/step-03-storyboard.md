# Step 3：分镜

Step 3 用镜头组组织同一场景或事件。目录固定为 `03_分镜脚本/镜头组-001/`，组内包含 `00_镜头组说明.md` 和全局唯一编号的 `镜头-001.md`。

一个镜头对应一次默认 15 秒的视频生成任务。镜头内部使用 1-4 个连续编号的“分镜 N”，默认 1-2 个。每镜默认选择一个最重要的关键帧；必要时可增加，但不得多于分镜数或超过 4 张。关键帧不默认绑定首帧或尾帧，Step 3 必须链接全部已选 Step 4 文件。
