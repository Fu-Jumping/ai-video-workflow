# 工作流总览

默认研究模式包含八个步骤：

0. 前期研究
1. 策划
2. 设定
3. 分镜
4. 图片提示词
5. 视频提示词
6. 执行方案
7. 发布物料

如果项目已有完整剧本，可用 `--start-from script` 或关闭 `workflow.research_step.enabled`，从 Step 1 到 Step 7 运行。

Step 3 与 Step 4 必须保持帧级对齐。Step 6 是执行方案系统，不只是清单。
