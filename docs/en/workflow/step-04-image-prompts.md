# Step 4: Image Prompts

Each file represents one stable keyframe and uses the `镜头-001-关键帧-01.md` naming contract. Metadata must record the shot group, shot ID, mapped storyboard segment, and key moment.

Every formal file keeps the fixed `快速导读`, `中文完整版本`, and `可复制提示词` sections. Both prompt versions must independently reconstruct the same frame and carry every semantic `@角色三视图` / `@场景图` reference declared by Step 3.
