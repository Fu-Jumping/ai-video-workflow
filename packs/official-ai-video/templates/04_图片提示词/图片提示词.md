# 图片提示词模板

## 文件用途

- 当前文件只服务一个关键帧和一个稳定画面状态。
- 默认面向项目初始化时选择并记录的图片平台。
- 正文必须让模型单看这一份文件就能理解完整画面。

## 维护元信息

- 镜头组：group-001
- 镜头编号：shot-001
- 场景名称：
- 镜头名称：
- 关键帧名称：
- 对应分镜：分镜 1
- 关键时刻：
- 上游分镜卡：
- 必带参考资产：

## 快速导读

- 画面内容：
- 机位关系：
- 主体状态：

> 这里只写纯视觉事实，不写维护者说明或上下文提示。

## 中文完整版本

```text
参考 @角色名三视图、@场景名场景图。

请把这里替换成最终中文提示词。正文默认写成一到两段连续自然语言，先写这张图此刻正在发生什么，再写前景、中景、背景、空间方向、光线、关键物件和画面约束。

避免：
```

## 可复制提示词

```text
参考 @角色名三视图、@场景名场景图。

请把这里替换成可直接复制到图片生成平台的最终中文提示词。提示词必须描述一个稳定可见画面，不依赖前文、不依赖内部代号、不同时描述动作前态和动作后态。

避免：
```

## 平台执行参数（仅当默认图片平台需要平台特定参数时必填；当前仅 midjourney）

> midjourney 项目：可复制提示词用英文（七要素 + 参数行，默认实拍电影感收尾、风格方向可按画面切换），正文 ≤1024 字符；中文完整版本仍为中文 ≥180 字；平台事实见 `packs/official-ai-video/workflow/indexes/platform-midjourney-v82.md`。

- 平台：
- 版本：
- 画幅：
- 风格方向（默认实拍电影感，可覆盖；备选方案库见 `indexes/platform-midjourney-v82.md` 第 2 部分）：
- 风格参数：
- stylize：
- 参考资产上传方式：
- 负面约束执行方式：

## 中文自检

- [ ] 完整描述了一个稳定画面。
- [ ] 写清了主体、空间、光线和关键物件。
- [ ] 已携带 Step 3 声明的全部角色三视图与场景图引用。
- [ ] 写清了避免项。
- [ ] 不依赖前文或项目上下文。
- [ ] 没有使用内部代号。
- [ ] 当前文件只服务一个关键帧和一个稳定画面状态。
- [ ] （midjourney 项目）已写 `## 平台执行参数`，含 `--v 8.2` 与 `--ar`。
- [ ] （midjourney 项目）可复制提示词为英文且正文 ≤1024 字符。
- [ ] （midjourney 项目）风格方向与画面内容一致（默认实拍电影感；插画/水墨等其他方向已显式声明）。
- [ ] （midjourney 项目）避免段只写单词级否定，未使用 `--cref` / `--cw` / `--q` / `::`。
