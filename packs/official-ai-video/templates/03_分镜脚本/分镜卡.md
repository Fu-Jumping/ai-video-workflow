# 分镜卡模板

## 文件用途

- 当前文件只描述一个镜头。
- 分镜是步骤四图片提示词的画面事实源。
- 文档内引用其他文件时只使用相对路径。

## 镜头组与基础信息

- 镜头组编号：group-001
- 镜头组目录：镜头组-001
- 镜头组说明：[[00_镜头组说明.md]]

- 镜头编号：
- 场景：
- 角色：
- 目标时长：15 秒（可显式缩短，不得超过 15 秒）
- 情绪方向：

## 分镜编排

- 分镜数量：1-2 个，复杂内容最多 4 个
- 每个分镜必须是独立的 `### 分镜 N` 标题段落（校验器按该标题识别分镜；不能用 `- 分镜 N：` 条目代替）：

### 分镜 1

景别、视角、起始画面、主体和关键动作。

### 分镜 2

景别、视角、承接状态、主体和关键动作。（可选，按分镜数量填写，最多分镜 4）

> 分镜编号必须从 1 连续到实际数量；Step 5 可复制提示词逐项改写为“镜头1：/镜头2：”。

## 画面描述

- 构图：
- 机位：
- 景别：
- 主体位置：
- 前景：
- 中景：
- 背景：
- 光线：
- 关键道具：

## 参考资产要求

- 主角色三视图：
- 场景设定图：
- 其他参考资产：

## 动作与节奏

- 起始状态：
- 核心动作：
- 结束状态：
- 节奏：
- 禁止压缩的动作：

## 关键帧选择

- 选择数量：默认 1 张；必要时可增加，但不得多于分镜数量或超过 4 张
- 关键帧 01：对应分镜、关键时刻、稳定画面状态、选择理由
- 关键帧 02：对应分镜、关键时刻、稳定画面状态、选择理由（可选）
- 关键帧 03：对应分镜、关键时刻、稳定画面状态、选择理由（可选）
- 关键帧 04：对应分镜、关键时刻、稳定画面状态、选择理由（可选）

> 关键帧不默认绑定首帧或尾帧；文件编号只表示顺序，实际对应分镜和时刻必须写在 Step 4 元信息中。

## 下游文件

- 对应图片提示词：填写为 Markdown 相对链接，指向 `../../04_图片提示词/镜头组-001/镜头-001-关键帧-01.md`
- 对应视频提示词：填写为 Markdown 相对链接，指向 `../../05_视频提示词/镜头组-001/镜头-001.md`

> 下游文件必须使用 Markdown 相对链接，不能只写反引号路径，否则 verify 会报 missing-step3-step4-link。

## 继承约束

- 步骤四必须继承的稳定画面：
- 步骤五必须继承的画面连续性：
- 禁止新增的内容：
